{
  "skill_name": "commit",
  "evals": [
    {
      "id": "focused-local-commit",
      "prompt": "Using the supplied worktree snapshot, give the exact safe staging and staged-diff verification sequence for one local validation commit. Leave unrelated work untouched. Because this is a text-only snapshot, do not claim that commands or a commit actually ran.",
      "expected_output": "Selects only scripts/validate.py and tests/test_validate.py, excludes README.md and coverage.xml, includes status and exact cached-diff review before the local commit, uses a focused message, and does not claim execution or push.",
      "should_trigger": true,
      "files": ["evals/fixtures/focused-worktree.txt"],
      "assertions": [
        "The sequence stages only scripts/validate.py and tests/test_validate.py, then reviews git diff --cached --check and the exact cached patch before committing",
        "README.md and coverage.xml remain uncommitted, no push is attempted, and the response does not falsely claim execution"
      ],
      "tags": ["git", "staging", "commit"]
    },
    {
      "id": "split-mixed-worktree",
      "prompt": "Prepare commits from this mixed worktree: the cache fix belongs now, but my half-finished README rewrite must remain uncommitted.",
      "expected_output": "Separates semantic concerns, inspects new and modified files, stages only the cache fix and its direct tests, and names the README work as intentionally untouched.",
      "should_trigger": true,
      "assertions": [
        "The unrelated README rewrite is preserved outside the staged change set",
        "The proposed commit groups the cache behavior with only its directly related tests or documentation"
      ],
      "tags": ["git", "mixed-worktree"]
    },
    {
      "id": "staged-diff-safety",
      "prompt": "Review what is staged, run the repository's required checks, and commit it if the staged patch is coherent and safe.",
      "expected_output": "Examines the staged patch rather than trusting status alone, checks for conflicts or secrets, runs scoped required validation, and either commits or reports a concrete blocker.",
      "should_trigger": true,
      "assertions": [
        "The exact staged content is reviewed for coherence, conflicts, and sensitive material",
        "A validation failure or unsafe staged file blocks the commit instead of being silently ignored"
      ],
      "tags": ["git", "verification"]
    },
    {
      "id": "near-miss-push-release-branch",
      "prompt": "Rebase the release branch, force-push it, and open the production pull request.",
      "expected_output": "Should not route to commit because branch rewriting, remote mutation, and pull-request operations are the primary workflow.",
      "should_trigger": false,
      "tags": ["near-miss", "remote-git"]
    },
    {
      "id": "near-miss-debug-regression",
      "prompt": "Find the root cause of this pagination regression, implement the fix, and add a targeted test.",
      "expected_output": "Should route to senior-developer because diagnosis and implementation are primary; commit may follow only after the work is complete.",
      "should_trigger": false,
      "tags": ["near-miss", "debugging"]
    }
  ]
}
