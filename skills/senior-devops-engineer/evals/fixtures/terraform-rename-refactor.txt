# modules/database/main.tf (current, deployed in production via GCS state)
resource "google_sql_database_instance" "main" {
  name             = "billing-db"
  database_version = "POSTGRES_15"
  region           = var.region

  settings {
    tier = "db-custom-2-7680"
    ip_configuration {
      ipv4_enabled    = false
      private_network = var.network_id
    }
  }

  deletion_protection = true
}

# Change request: as part of a module restructure this resource must become
# google_sql_database_instance "primary" (same module), with zero destroy or
# replacement of the deployed production instance.
